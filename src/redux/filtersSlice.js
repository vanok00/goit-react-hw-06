const initialState = 
{
    filters: {
          name: ""
      }
  }

export const changeFilterReducer = (state = {initialState}, action) => {
    return state;
}

export const selectNameFilter = () => {};